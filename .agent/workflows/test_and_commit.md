---
description: Run tests and commit changes
---
Run tests with coverage and commit if successful.

// turbo-all
1. Run tests with coverage
   bun test --coverage

2. Check for git changes
   git status

3. Add changes
   git add .
